-- Start Function. Run when the script component, or an object with this component attached, is first created
function Start()
	
	
end

-- Update Function. Run once per frame
function Update()
	
	--movementSpeed = 5
	--position = { Transform.Position.x, Transform.Position.y, Transform.Position.z }
	--
	--if (input.keyPressed(input.keys.w)) then
	--	logger.trace("W")
	--	position[3] = position[3] + (movementSpeed * time.timestep)
	--end
	--if (input.keyPressed(input.keys.a)) then
	--	logger.trace("A")
	--	position[1] = position[1] + (movementSpeed * time.timestep)
	--end
	--if (input.keyPressed(input.keys.s)) then
	--	logger.trace("S")
	--	position[3] = position[3] - (movementSpeed * time.timestep)
	--end
	--if (input.keyPressed(input.keys.d)) then
	--	logger.trace("D")
	--	position[1] = position[1] - (movementSpeed * time.timestep)
	--end
	--
	--transform.position.set(position[1], position[2], position[3])
end