#include "windowTests.h"

// Commented out, tests don't work due to error using GLFWWindowImpl class
//TEST(Window, EventCallback)
//{	
//	// Set the window event callback function
//	window.setEventCallback(eventCallbackFunc);
//	// Call the callback function with an event
//	Engine::KeyPressedEvent e(0, 0);
//	window.m_callback(e);
//	// If correct function is called, worked will be true
//	EXPECT_TRUE(worked);
//}